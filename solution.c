#include <stdio.h>

/*
 * isEven - returns 1 if x is even, and 0 otherwise
 *   Legal ops: ! ~ & ^ | +
 *   Max ops: 5
 *   Rating: 1
 */
int isEven(int x)
{
    // Check the least significant bit; if it's 0, x is even
}

int main()
{
    printf("%d\n", isEven(4)); // Should print 1 (even)
    return 0;
}
